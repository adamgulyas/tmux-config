# Set true color
set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Set prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Move between windows vim style
bind -n M-H previous-window
bind -n M-L next-window

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'dreamsofcode-io/catppuccin-tmux'
set -g @plugin '2kabhishek/tmux2k'
set -g @plugin 'tmux-plugins/tmux-yank'

# use a theme
set -g @tmux2k-theme 'monokai'

# to show icons only
set -g @tmux2k-icons-only true

# to set powerline symbols
# set -g @tmux2k-right-sep  # alternate right status bar sep
# set -g @tmux2k-win-right-sep  # alternate window right sep

# to not show powerline
set -g @tmux2k-show-powerline true

# set start icon, accpets: `session`, 'window`, or any string
set -g @tmux2k-start-icon ""

# set the left and right plugin sections
set -g @tmux2k-left-plugins "git cpu ram"
set -g @tmux2k-right-plugins "battery network time"

# set -g @tmux2k-[plugin-name]-colors "[background] [foreground]"
# set -g @tmux2k-git-colors "black green"
# set -g @tmux2k-cpu-colors "black blue"
# set -g @tmux2k-ram-colors "black yellow"
# set -g @tmux2k-battery-colors "black light_purple"
# set -g @tmux2k-network-colors "black purple" 
# set -g @tmux2k-time-colors "black light_blue"

# to enable compact window list size
set -g @tmux2k-compact-windows false

# change refresh rate
# set -g @tmux2k-refresh-rate 5

# weather scale
set -g @tmux2k-show-fahrenheit false

# 24 hour time
# set -g @tmux2k-military-time false

# network interface to watch
# set -g @tmux2k-network-name "wlo1"

# # set -g @tmux2k-# to customize plugin colors
# Tokyo Night Colors
# set -g @tmux2k-text '#15161E'
# set -g @tmux2k-bg_main '#1a1b26'
# set -g @tmux2k-bg_alt '#c0caf5'
# set -g @tmux2k-black  '#15161E'
# set -g @tmux2k-white '#a9b1d6'
# set -g @tmux2k-red '#f7768e'
# set -g @tmux2k-light_red '#f7768e'
# set -g @tmux2k-green '#9ece6a'
# set -g @tmux2k-light_green '#9ece6a'
# set -g @tmux2k-blue '#7aa2f7'
# set -g @tmux2k-light_blue '#7aa2f7'
# set -g @tmux2k-yellow '#e0af68'
# set -g @tmux2k-light_yellow '#e0af68'
# set -g @tmux2k-purple '#bb9af7'
# set -g @tmux2k-light_purple '#bb9af7'


# Set vi mode
set-window-option -g mode-keys vi

# Keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind - split-window -v -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"

run '~/.tmux/plugins/tpm/tpm'
